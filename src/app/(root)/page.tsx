import TestOnline from "@/components/layout/profile/TestOnline";
import React from "react";
export const dynamic = "force-dynamic";

const page = async () => {
  return (
    <div>
      <TestOnline />
    </div>
  );
};

export default page;
